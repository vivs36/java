/*Write a Java program to check whether an entered character is a digit / capital
letter or a small letter. Take the input using Scanner class*/
import java.util.Scanner;
class Question_1
{
    static void Check()
    {
        Scanner sc=new Scanner(System.in);
        System.out.println("Enter a Character");
        char ch=sc.next().charAt(0);
        if (ch>=65 && ch<=90)//ASCII codes
         System.out.println("Capital Letter");
         else if (ch>=97 && ch<=122)//ASCII codes
         System.out.println("Small Letter");
         else if (ch>=48 && ch<=57)//ASCII codes
         System.out.println("Digit");
    }
}